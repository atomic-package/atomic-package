const assert = require("power-assert");
const atomicPackage = require("../../docs/static/js/atomic-package.js");

describe('ts/controller/atomic_package_view.ts', function () {
  const view = atomicPackage.AP.getView();

});