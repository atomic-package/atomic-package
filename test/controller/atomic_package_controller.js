const assert = require("power-assert");
const atomicPackage = require("../../docs/static/js/atomic-package.js");

describe('ts/controller/atomic_package_controller.ts', function () {
  const controller = atomicPackage.AP.getController();

});